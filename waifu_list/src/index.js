import React from "react";
import ReactDOM from "react-dom"; // For rendering the element in the actual DOM
import "bootstrap/dist/css/bootstrap.css";
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />,
);